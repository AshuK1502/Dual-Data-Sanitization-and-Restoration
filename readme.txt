==> open the given folder on terminal 
==> run the following command to execute the dashboard.py
		streamlit run dashboard.py

==> make sure streamlit, numpy , pandas and scikit-learn is installed on your IDE.
		If they are not installed, run the command to install them :
			pip install pandas numpy scikit-learn streamlit

